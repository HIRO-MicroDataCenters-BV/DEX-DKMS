issuer: "http://localhost:8900"
host: "0.0.0.0"
port: 8900

client_id: "dex-client"
client_secret: "dex-secret"

# Path to RSA private key PEM. If null, generates ephemeral key at startup.
rsa_key_path: null

auth_code_ttl: 300
id_token_ttl: 3600

# KERI/DKMS witness URLs for OOBI resolution
witnesses:
  - "http://localhost:3232"
  - "http://localhost:3233"
  - "http://localhost:3234"

# Watcher URL
watcher: "http://localhost:3235"
